# nerdtree-yank-node-plugin

NERDTree plugin for yank a node element.

## Instalation

### Plug
```
Plug 'lourenci/nerdtree-yank-node-plugin', { 'tag': '*' }
```

## Usage

Just press `yy` to yank the relative path of the selected node.
